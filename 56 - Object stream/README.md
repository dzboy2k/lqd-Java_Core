### Đọc ghi object vào file trong Java
- Để đọc = ghi đối tượng vào file ta cần thực hiện như sau:
  - Các đối tượng cần phải implements java.io.Serializable.
  - Sử dụng các class ObjectInputStream/ObjectOutputStream.